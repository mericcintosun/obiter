# DELTA (phase 8)

## Diagnosis

1. SHOT /Users/mericcintosun/farm-hackathon/data/exports/devpost-syndicate-by-maximor/shots-faz8-mtq84myq/before-desktop-fold.png | region: the whole first viewport at 1440 wide, from the 56px header rule down to the crop | defect: the fold holds four things and none of them is the product: a gray 11px kicker, a three-line black serif h1 running from y 200 to y 440, and a five-line gray standfirst ending at y 680, all on the warm #faf6ee ground. No number, no state of the close, no button, no link into anything. A judge's first ten seconds contain zero evidence that this app runs. | source: `app/page.tsx`, the CTA lives in the `<div className="mt-8">` block after the second `<h2>` | violates: phase goal, the cold open, and ARCHETYPE L3 editorial, whose fold is supposed to carry a dateline and the state of the case.

2. SHOT /Users/mericcintosun/farm-hackathon/data/exports/devpost-syndicate-by-maximor/shots-faz8-mtq84myq/before-desktop-fold.png | region: the standfirst paragraph, y 495 to 690, directly under the h1 | defect: the loudest paragraph on the page is set in the muted gray that `--muted-foreground` mixes (60 percent ink into ground), so it sits visibly lighter than the pure-ink body copy that starts at y 880 under "The part that never got automated". The standfirst reads as placeholder filler and the body reads as the real text, which is exactly backwards. | source: `app/page.tsx:45`, `className="mt-6 text-lg leading-relaxed text-muted-foreground"` | violates: IDENTITY INK `#221d16` as the reading color; a letterpress standfirst is ink, not a gray wash.

3. SHOT /Users/mericcintosun/farm-hackathon/data/exports/devpost-syndicate-by-maximor/shots-faz8-mtq84myq/before-desktop-fold.png | region: the header, left end, x 320 to 440, y 20 to 55 | defect: the raster mark renders 26px square with no frame on the same #faf6ee the header is painted in, so the pale beige seal art has no edge and reads as a stray favicon that failed to load. The wordmark "Obiter" sits alone next to it with nothing naming what the product is. | source: `app/layout.tsx:58-68` | violates: HEADER solid ground with a border, and the ONE MARK RULE's framing clause; the mark is the masthead and is being rendered as a sprite.

4. SHOT /Users/mericcintosun/farm-hackathon/data/exports/devpost-syndicate-by-maximor/shots-faz8-mtq84myq/before-desktop-page-1.png | region: y 1730 to 1815, the cobalt #2d4fd1 button and the gray sentence under it | defect: the one and only way into the product is a cobalt button that appears after two full `<h2>` sections and an illustration, roughly two and a half screens below the fold, with a gray 13px sentence under it doing the work the fold should have done ("24 open exceptions across six patterns, seeded and ready"). | source: `app/page.tsx`, the `<div className="mt-8">` CTA block | violates: phase goal, the primary CTA into DEMO step 1 must sit in the first viewport.

5. SHOT /Users/mericcintosun/farm-hackathon/data/exports/devpost-syndicate-by-maximor/shots-faz8-mtq84myq/before-desktop-page-2.png | region: the "Built on" section, y 1425 to 1625, the last block before the footer rule | defect: the entire technical claim is one five-line ink paragraph with no rules, no entries and no scanning structure. Agent Orchestrator, which is the eliminatory judging requirement, is the last clause of the last sentence of the last paragraph of the page. Nothing here is a ledger entry. | source: `app/page.tsx:181-187`, the "Built on" `<p>` | violates: ARCHETYPE L3, which puts proof in ruled ledger rows in the body face.

6. SHOT /Users/mericcintosun/farm-hackathon/data/exports/devpost-syndicate-by-maximor/shots-faz8-mtq84myq/before-mobile-fold.png | region: the entire 390 wide viewport | defect: at 390 the h1 alone occupies y 320 to 555 and the visible screen ends part way through the gray standfirst at y 1060. There is no CTA, no figure, no number and no rule in the first screen; the first thing a phone user can act on is off screen entirely. | source: `app/page.tsx`, the same CTA block | violates: phase goal, first-viewport CTA at 390.

7. SHOT /Users/mericcintosun/farm-hackathon/data/exports/devpost-syndicate-by-maximor/shots-faz8-mtq84myq/before-desktop-page-1.png | region: the two serif headings at y 655 and y 1405, "The part that never got automated" and "What Obiter does with one decision" | defect: each `<h2>` sits on nothing but whitespace, with no section mark, no numbering and no rule, so the page reads as an undifferentiated stack of serif lines and ink paragraphs rather than as a case report with parts. There is no way to tell from the page where you are in the argument. | source: `app/page.tsx:54`, `:90`, `:124`, `:169`, `:180`, all `<h2 className="mt-N text-2xl">` | violates: ARCHETYPE L3 editorial, which carries running heads over its sections.

8. SHOT /Users/mericcintosun/farm-hackathon/data/exports/devpost-syndicate-by-maximor/shots-faz8-mtq84myq/before-desktop-page-1.png | region: the ledger illustration, y 1075 to 1290, framed in a hairline #e9e0cd border on the #f3ecdf surface | defect: the one picture on the page, the ruled queue with three cobalt and forest seals in the margin, is buried below two paragraphs of body copy in the middle of section one, where it anchors nothing. The fold above it has no image at all. | source: `app/page.tsx:72-88`, the `<figure className="mt-10">` | violates: phase goal, the figure should anchor the first screen's spill.

9. SHOT /Users/mericcintosun/farm-hackathon/data/exports/devpost-syndicate-by-maximor/shots-faz8-mtq84myq/before-mobile-page-2.png | region: the compiled precedent block, y 990 to 1400, a solid #f3ecdf slab with the mono JSON overflowing its right edge at "counterparty_group", | defect: it is the only filled box on an otherwise fully ruled page, and at 390 the JSON is clipped mid-token so a phone reader sees a truncated object. | source: `app/page.tsx:131-141`, the `<Card>` around the `<pre className="font-mono">` | violates: nothing outright, mono here is the one sanctioned role, but the filled slab is the loudest non-ruled object on an L3 page. Kept, see Kept.

10. SHOT /Users/mericcintosun/farm-hackathon/data/exports/devpost-syndicate-by-maximor/shots-faz8-mtq84myq/before-desktop-page-2.png | region: y 920 to 960, the disclosure line "How the compiler is wired, for anyone reading the code" between two hairline rules | defect: it renders as a bare native triangle glyph plus body text, with no label face and no affordance beyond the glyph, so the one place the page offers depth looks like an accident of the browser rather than a designed row. | source: `app/page.tsx:143-167`, the `<details className="obiter-rule ...">` | violates: nothing in the banned list. Kept, see Kept.

## Changes

| id | claim (one sentence) | file | proof-gone | proof-new |
| --- | --- | --- | --- | --- |
| C1 | The standfirst becomes a full-ink lede set by a new token-only `.obiter-lede` class instead of a gray wash. | `app/globals.css`, `app/page.tsx` | `mt-6 text-lg leading-relaxed text-muted-foreground` | `obiter-lede` |
| C2 | The header mark gets a hairline `border border-border` tile on `bg-ground` at 30px and the wordmark gains a small caps dateline sibling naming the product. | `app/layout.tsx`, `app/globals.css` | `h-[26px] w-[26px] object-contain` | `obiter-masthead-mark` |
| C3 | The footer becomes a ruled block with a link row above the two existing paragraphs: close queue, source, SECURITY.md, hackathon page. | `app/layout.tsx` | `max-w-5xl px-5 py-8 text-sm text-muted-foreground` | `obiter-footer-links` |
| C4 | The first viewport carries the kicker, the h1, the ink lede, a dateline, the close state in one sentence with live figures, and the CTA into `/close`. | `app/page.tsx`, `app/globals.css` | `text-[clamp(2.4rem,6vw,3.6rem)]` | `obiter-dateline` |
| C5 | Every `<h2>` gains a running head in the label face so the page scans as a case report. | `app/page.tsx`, `app/globals.css` | `<h2 className="mt-10 text-2xl">The part that never got automated` | `obiter-runninghead` |
| C6 | The ledger illustration moves directly under the CTA to anchor the first screen's spill, keeping `unoptimized` and its alt text. | `app/page.tsx` | `<figure className="mt-10">` | `<figure className="mt-9">` |
| C7 | The "Built on" paragraph becomes a ruled `<dl>` of five entries in the body face, Agent Orchestrator first. | `app/page.tsx` | `Next.js 15 and TypeScript on Vercel. Claude for the one compilation step` | `<dt className="font-medium` |
| C8 | The autonomy meter gets a labeled ruled frame with real progressbar semantics instead of a bare `aria-hidden` bar. | `components/close-queue.tsx` | `className="mt-5 h-2 w-full border border-border bg-surface" aria-hidden` | `role="progressbar"` |
| C9 | The queue gains a column header row in the label face, hidden under `sm` the way the measures panel already hides its own. | `components/close-queue.tsx` | `<ul className="obiter-queue mt-2">` | `obiter-queue-head` |
| C10 | Closed rows carry a cobalt seal margin instead of a flat opacity drop. | `components/close-queue.tsx` | `isClosed && "opacity-70"` | `border-l-2 border-seal` |
| C11 | The measures panel is framed as the same ledger object as the queue, and a global `:focus-visible` outline built on `var(--ring)` lands with the label-family rules. | `components/close-measures.tsx`, `app/globals.css` | `.obiter-label {` | `:focus-visible` |

## Kept

- Diagnosis 9, the filled surface card around the compiled precedent JSON: mono there is IDENTITY's one sanctioned role and DEMO.md step 3 quotes the block, so the slab stays and only its horizontal scroll behaviour is left as is.
- Diagnosis 10, the bare `<details>` disclosure line: restyling it buys nothing a judge sees in the first ten seconds and this phase's budget goes to the fold and the queue.

## Final grep counts (run at slice 6 with the Grep tool)

proof-gone counted repo-wide excluding this file and `HANDOFF.md`; proof-new
counted under `app/` and `components/`.

| id | proof-gone hits | proof-new hits |
| --- | --- | --- |
| C1 | 0 | 2 (`app/page.tsx`, `app/globals.css`) |
| C2 | 0 | 2 (`app/layout.tsx`, `app/globals.css`) |
| C3 | 0 | 2 (`app/layout.tsx`, `app/globals.css`) |
| C4 | 0 | 3 (`app/layout.tsx`, `app/page.tsx`, `app/globals.css`) |
| C5 | 0 | 7 (5 in `app/page.tsx`, 2 in `app/globals.css`) |
| C6 | 0 | 1 (`app/page.tsx`) |
| C7 | 0 | 5 (`app/page.tsx`) |
| C8 | 0 | 1 (`components/close-queue.tsx`) |
| C9 | 0 | 2 (`components/close-queue.tsx`, `app/globals.css`) |
| C10 | 0 | 1 (`components/close-queue.tsx`) |
| C11 | 0 | 1 (`app/globals.css`) |

Tripwires, same pass: the 24 banned hexes, `fade-up`, `float-y`, `glow-pulse`,
`caret-blink`, `pulse-dot`, `role="tablist"`, `animate-pulse`, `backdrop-blur`,
`rounded-xl`, `rounded-2xl`, `rounded-full`, `--delay` and `--d:` all return
**zero** hits under `app/` and `components/`. `@keyframes` returns exactly two,
`obiter-wipe` and `obiter-stamp`, both in `app/globals.css`. Hex literals return
nine in `app/globals.css` and the rest in `app/icon.svg`, which CLAUDE.md
exempts; zero under `components/`.

## Header JSX, quoted verbatim (gate item 7)

```tsx
<Link href="/" className="flex items-center gap-3">
  <span className="obiter-masthead-mark">
    <Image
      src="/brand/logo.png"
      alt=""
      width={30}
      height={30}
      priority
      className="h-[30px] w-[30px] object-contain"
    />
  </span>
  <span className="flex flex-col leading-none">
    <span className="font-display text-lg tracking-tight">Obiter</span>
    {/* Hidden under sm: at 360 the nav takes most of the row and a
        tracked line this long would wrap the masthead onto a second
        line inside a 56px header. */}
    <span className="obiter-dateline mt-0.5 hidden sm:block">
      Case reporter for the close
    </span>
  </span>
</Link>
```

One `<Image>`, no second brand image, no `<svg>` and no `<img>` in the header.
